import TransactionContent from 'components/App/Transactions/Content'

const WalletTransactions: React.FC<React.PropsWithChildren> = () => {
  return <TransactionContent className="mt-4" />
}

export default WalletTransactions
